# 2026-01-23 변경사항 및 트러블슈팅 가이드

## 📋 목차
1. [구현한 변경사항](#구현한-변경사항)
2. [발생한 문제와 해결방법](#발생한-문제와-해결방법)
3. [사용한 Bash 명령어](#사용한-bash-명령어)
4. [파일 변경 목록](#파일-변경-목록)
5. [초보자용 가이드](#초보자용-가이드)

---

## 구현한 변경사항

### 1. 닉네임 표시 기능 추가 ✅

**문제**: 게시글 목록에서 "유저-1", "유저-2"로 표시됨

**해결책**: 사용자의 실제 닉네임(name 필드)을 표시

#### 백엔드 변경사항:

**신규 파일: `PostDTO.java`**
```java
// 위치: backend/src/main/java/com/ej2/dto/PostDTO.java
// 목적: Post 엔티티에 작성자 닉네임을 추가한 DTO
public class PostDTO {
    private String authorNickname;  // 작성자 닉네임
    // ... 기타 필드
}
```

**수정 파일: `PostService.java`**
```java
// UserRepository 주입 추가
@Autowired
private UserRepository userRepository;

// Post → PostDTO 변환 메서드
private String getAuthorNickname(Post post) {
    User user = userRepository.findById(post.getUserId());
    if (user != null) {
        return user.getName();  // username이 아닌 name 사용
    }
    return "Unknown User";
}
```

**수정 파일: `PostController.java`**
```java
// Post 대신 PostDTO를 반환하도록 변경
@GetMapping
public ResponseEntity<List<PostDTO>> getAllPosts() {
    List<PostDTO> posts = postService.getAllPosts();
    return ResponseEntity.ok(posts);
}
```

#### 프론트엔드 변경사항:

**수정 파일: `MainPage.js`**
```javascript
// userId 표시 → authorNickname 표시로 변경
<span className="post-item-author">
  {board.isAnonymous
    ? (post.anonymousId || '匿名')
    : (post.authorNickname || 'Unknown User')}
</span>
```

동일한 변경사항을 `PostDetailPage.js`와 `PostListPage.js`에도 적용

---

### 2. 날짜/시간 표시 수정 ✅

**문제**: 날짜가 배열 형식 `[2026,1,23,1,56,2]`로 표시됨

**해결책**: Jackson 설정을 변경하여 ISO 문자열 형식으로 변환

**수정 파일: `WebConfig.java`**
```java
@Override
public void configureMessageConverters(List<HttpMessageConverter<?>> converters) {
    ObjectMapper objectMapper = new ObjectMapper();
    JavaTimeModule javaTimeModule = new JavaTimeModule();
    objectMapper.registerModule(javaTimeModule);

    // 날짜를 배열이 아닌 ISO 문자열로 직렬화
    objectMapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);

    MappingJackson2HttpMessageConverter converter = new MappingJackson2HttpMessageConverter();
    converter.setObjectMapper(objectMapper);
    converters.add(converter);
}
```

**결과**: `"2026-01-23T01:56:02"` 형식으로 반환됨

---

### 3. 인기글 통계 표시 형식 변경 ✅

**변경 전**:
```
いいね 71
コメント 0
```

**변경 후**:
```
👁 460 / 👍 71 / 💬 0
```

**수정 파일: `MainPage.js`**
```javascript
<div className="popular-stats-eta">
  👁 {post.viewCount || 0}
  <span className="stat-separator"> / </span>
  👍 {post.likeCount || 0}
  <span className="stat-separator"> / </span>
  💬 {post.commentCount || 0}
</div>
```

---

### 4. EJ2 로고 스타일링 ✅

**구현 내용**:
- 왼쪽에 10px 패딩 추가
- 클릭 시 홈페이지로 이동
- 호버 효과 (색상 변경 + 확대)

**수정 파일: `App.js`**
```javascript
// Link 컴포넌트로 감싸기
<Link to="/" className="nav-logo-link">
  <h1 className="nav-logo">EJ2</h1>
</Link>
```

**수정 파일: `App.css`**
```css
.nav-logo-link {
  text-decoration: none;
  margin-left: 10px;
}

.nav-logo:hover {
  color: #2563eb;
  transform: scale(1.05);
}
```

---

### 5. 간단게시판 메뉴 삭제 ✅

**수정 파일: `App.js`**
```javascript
// 삭제된 부분:
// <li className="nav-item">
//   <Link to="/simple-board" className="nav-link">簡単掲示板</Link>
// </li>

// <Route path="/simple-board" element={<BoardPage />} />
```

---

### 6. 게시글 삭제 기능 추가 ✅

**구현 내용**: 자신의 게시글만 삭제 가능

**수정 파일: `PostDetailPage.js`**
```javascript
const canModifyPost = () => {
  const user = JSON.parse(localStorage.getItem('user') || '{}');
  return user.id && post && user.id === post.userId;
};

const handleDelete = async () => {
  if (!window.confirm('정말 이 게시글을 삭제하시겠습니까?')) {
    return;
  }

  try {
    await axios.delete(`/api/posts/${postId}`);
    alert('게시글이 삭제되었습니다');
    navigate(`/boards/${boardId}/posts`, { state: { board } });
  } catch (error) {
    console.error('게시글 삭제 실패:', error);
    alert('게시글 삭제에 실패했습니다');
  }
};
```

**UI 코드**:
```javascript
{canModifyPost() && (
  <button className="delete-button" onClick={handleDelete}>
    🗑️ 削除
  </button>
)}
```

---

### 7. 게시글 수정 기능 추가 ✅

**구현 내용**: 자신의 게시글을 수정 가능

#### 신규 파일: `PostEditPage.js`
```javascript
// 위치: frontend/src/pages/Board/PostEditPage.js
// PostWritePage의 스타일을 재사용
// PUT /api/posts/{id} API 호출
```

**주요 기능**:
- 제목과 내용 수정
- 권한 검증 (작성자만 수정 가능)
- 취소 시 확인 다이얼로그

**수정 파일: `PostDetailPage.js`**
```javascript
const handleEdit = () => {
  navigate(`/boards/${boardId}/posts/${postId}/edit`, {
    state: { board, post }
  });
};

{canModifyPost() && (
  <>
    <button className="edit-button" onClick={handleEdit}>
      ✏️ 編集
    </button>
    <button className="delete-button" onClick={handleDelete}>
      🗑️ 削除
    </button>
  </>
)}
```

**수정 파일: `App.js`**
```javascript
import PostEditPage from './pages/Board/PostEditPage';

// 라우트 추가
<Route path="/boards/:boardId/posts/:postId/edit" element={<PostEditPage />} />
```

---

## 발생한 문제와 해결방법

### 문제 1: 닉네임이 "newuser1"로 표시됨

#### 🔴 증상
게시글 목록과 상세 페이지에서 작성자가 "newuser1", "testuser1" 같은 **로그인 ID**로 표시됨

```
작성자: newuser1  ❌ (원하는 결과 아님)
원하는 결과: sfdsds  ✅
```

#### 🔍 문제 발생 과정

1. **프론트엔드 확인**
   ```javascript
   // MainPage.js, PostDetailPage.js
   {post.authorNickname || 'Unknown User'}
   ```
   프론트엔드는 `authorNickname` 필드를 올바르게 사용하고 있음

2. **API 응답 확인**
   ```bash
   curl http://localhost:8080/api/posts/board/1 | python3 -m json.tool
   ```
   출력:
   ```json
   {
     "authorNickname": "newuser1"  // ❌ 로그인 ID가 표시됨
   }
   ```

3. **백엔드 코드 확인**
   ```java
   // PostService.java - 문제가 있는 코드
   private String getAuthorNickname(Post post) {
       User user = userRepository.findById(post.getUserId());
       if (user != null) {
           return user.getUsername();  // ❌ 여기가 문제!
       }
       return "Unknown User";
   }
   ```

4. **User 엔티티 구조 확인**
   ```java
   // User.java
   private String username;  // 로그인 ID (예: newuser1)
   private String name;      // 실제 이름/닉네임 (예: sfdsds)
   private String email;     // 이메일 (예: test@example.com)
   ```

#### ✅ 해결 방법

**변경 전**:
```java
return user.getUsername();  // "newuser1" (로그인 ID)
```

**변경 후**:
```java
return user.getName();  // "sfdsds" (실제 이름)
```

#### 🧪 해결 확인

```bash
# 1. 백엔드 재빌드
docker-compose up --build -d backend

# 2. API 응답 확인
curl -s http://localhost:8080/api/posts/board/1 | \
  python3 -c "import sys, json; data=json.load(sys.stdin); \
  print(data[0]['authorNickname'])"

# 출력: sfdsds  ✅
```

#### 📚 배운 점
- User 엔티티의 각 필드 용도를 정확히 파악해야 함
- `username` = 로그인 ID (시스템 내부용)
- `name` = 표시용 이름 (사용자에게 보여줄 이름)
- `email` = 이메일 주소

---

### 문제 2: 날짜가 배열 `[2026,1,23,1,56,2]`로 표시됨

#### 🔴 증상
API 응답에서 날짜가 배열 형태로 반환되어 프론트엔드에서 "---"로 표시됨

```json
{
  "createdAt": [2026, 1, 23, 1, 56, 2]  ❌
}
```

프론트엔드에서:
```javascript
const date = new Date([2026, 1, 23, 1, 56, 2]);
// NaN 반환 → "---" 표시
```

#### 🔍 문제 발생 과정

1. **API 응답 확인**
   ```bash
   curl -s http://localhost:8080/api/posts/1 | python3 -m json.tool
   ```
   ```json
   {
     "id": 1,
     "title": "테스트",
     "createdAt": [2026, 1, 23, 1, 56, 2],  // ❌ 배열 형태
     "updatedAt": [2026, 1, 23, 1, 56, 2]
   }
   ```

2. **Jackson 기본 동작 확인**
   - Java 8의 `LocalDateTime`은 기본적으로 **타임스탬프 배열**로 직렬화됨
   - 이는 `WRITE_DATES_AS_TIMESTAMPS` 기능이 활성화되어 있기 때문

3. **프론트엔드 파싱 실패**
   ```javascript
   const date = new Date([2026, 1, 23, 1, 56, 2]);
   console.log(date);  // Invalid Date
   console.log(isNaN(date.getTime()));  // true
   ```

#### ✅ 해결 방법

**WebConfig.java 수정**:
```java
@Override
public void configureMessageConverters(List<HttpMessageConverter<?>> converters) {
    ObjectMapper objectMapper = new ObjectMapper();

    // Java 8 날짜/시간 API 지원
    JavaTimeModule javaTimeModule = new JavaTimeModule();
    objectMapper.registerModule(javaTimeModule);

    // ✅ 핵심: 배열 대신 ISO 문자열로 직렬화
    objectMapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);

    MappingJackson2HttpMessageConverter converter =
        new MappingJackson2HttpMessageConverter();
    converter.setObjectMapper(objectMapper);

    converters.add(converter);
}
```

#### 🧪 해결 확인

```bash
# 1. 백엔드 재빌드
docker-compose up --build -d backend

# 2. 변경 후 API 응답 확인
curl -s http://localhost:8080/api/posts/1 | python3 -m json.tool
```

**변경 전 응답**:
```json
{
  "createdAt": [2026, 1, 23, 1, 56, 2]  ❌
}
```

**변경 후 응답**:
```json
{
  "createdAt": "2026-01-23T01:56:02"  ✅ ISO 8601 형식
}
```

#### 🎯 프론트엔드에서 정상 파싱

```javascript
const date = new Date("2026-01-23T01:56:02");
console.log(date);  // Thu Jan 23 2026 01:56:02 GMT+0900

// getTimeAgo 함수가 정상 작동
const diffInMinutes = Math.floor((now - past) / (1000 * 60));
// 결과: "2시간 전", "1일 전" 등으로 표시됨 ✅
```

#### 📚 배운 점
- Jackson의 기본 동작을 이해해야 함
- `WRITE_DATES_AS_TIMESTAMPS` 설정 영향
- ISO 8601 형식이 국제 표준이며 JavaScript와 호환성이 좋음
- Java 8 날짜/시간 API 사용 시 `JavaTimeModule` 필수

---

### 문제 3: API 404 에러 발생

#### 🔴 증상
백엔드 API 호출 시 404 에러 발생

```bash
curl http://localhost:8080/ej2/api/posts/board/1
# 출력: 404 Not Found
```

#### 🔍 문제 발생 과정

1. **기대한 URL 구조**
   ```
   - web.xml의 context-path: /ej2
   - 예상 API URL: http://localhost:8080/ej2/api/posts
   ```

2. **Dockerfile 확인**
   ```dockerfile
   # backend/Dockerfile
   FROM tomcat:9-jdk8-openjdk
   RUN rm -rf /usr/local/tomcat/webapps/*

   # ❗ 여기가 핵심!
   COPY --from=build /app/target/ej2.war /usr/local/tomcat/webapps/ROOT.war
   ```

   WAR 파일이 `ROOT.war`로 배포됨 = 컨텍스트 패스가 `/`가 됨!

3. **실제 배포 상태 확인**
   ```bash
   docker exec spring-backend ls -la /usr/local/tomcat/webapps/
   ```
   ```
   drwxr-xr-x 3 root root 4096 Jan 23 10:00 ROOT
   ```
   `ROOT/` 디렉토리로 배포되어 있음

4. **Tomcat 배포 규칙**
   - `ej2.war` → 컨텍스트 패스: `/ej2`
   - `ROOT.war` → 컨텍스트 패스: `/` (루트)

   따라서 실제 API URL은: `http://localhost:8080/api/posts`

#### ✅ 해결 방법

**잘못된 URL**:
```bash
curl http://localhost:8080/ej2/api/posts
# 404 Not Found ❌
```

**올바른 URL**:
```bash
curl http://localhost:8080/api/posts
# 정상 응답 ✅
```

#### 🧪 해결 확인

```bash
# 1. API 응답 확인
curl -s http://localhost:8080/api/posts/board/1

# 2. JSON 정렬하여 확인
curl -s http://localhost:8080/api/posts/board/1 | python3 -m json.tool

# 3. 특정 필드만 추출
curl -s http://localhost:8080/api/posts/board/1 | \
  python3 -c "import sys, json; data=json.load(sys.stdin); \
  print(f'총 {len(data)}개의 게시글')"
```

**성공 응답**:
```json
[
  {
    "id": 28,
    "boardId": 1,
    "userId": 6,
    "authorNickname": "sfdsds",
    "title": "ㄴㅇㄹㅇㄴㄹ",
    "createdAt": "2026-01-23T01:56:02"
  }
]
```

#### 🔧 프론트엔드 설정 확인

**package.json**:
```json
{
  "proxy": "http://localhost:8080"
}
```
- 프론트엔드에서 `/api/*` 요청 시 자동으로 백엔드로 프록시됨
- 따라서 `/ej2` 접두사 불필요

**nginx.conf** (프로덕션):
```nginx
location /api {
    proxy_pass http://backend:8080;
}
```
- Nginx도 `/api`를 직접 백엔드로 전달

#### 📚 배운 점
- WAR 파일명이 배포 경로를 결정함
- `ROOT.war` = 루트 컨텍스트(`/`)로 배포
- `app.war` = `/app` 컨텍스트로 배포
- Dockerfile에서 배포 구조를 정확히 이해해야 함
- 프론트엔드 프록시 설정도 일치해야 함

---

### 문제 4: 빌드 시 긴 대기 시간

#### 🔴 증상
`docker-compose up --build` 실행 시 매번 5-10분 소요

#### 🔍 원인 분석

1. **Maven dependency 다운로드**
   ```dockerfile
   # 매번 실행됨
   RUN mvn dependency:go-offline
   ```

2. **npm install 실행**
   ```dockerfile
   # 매번 실행됨
   RUN npm install
   ```

#### ✅ 해결 방법

**Docker 레이어 캐싱 활용**:

변경사항이 없는 경우:
```bash
# 빌드 없이 재시작 (5초)
docker-compose restart backend

# 특정 컨테이너만 재빌드 (1-2분)
docker-compose up --build -d backend
```

변경사항이 있는 경우:
```bash
# 전체 재빌드 (5-10분)
docker-compose up --build -d
```

#### 📊 빌드 시간 비교

| 명령어 | 시간 | 사용 시점 |
|--------|------|----------|
| `docker-compose restart` | ~5초 | 설정 변경 시 |
| `docker-compose up -d` | ~10초 | 이미지가 있을 때 |
| `docker-compose up --build -d frontend` | ~1분 | 프론트엔드만 변경 |
| `docker-compose up --build -d backend` | ~2분 | 백엔드만 변경 |
| `docker-compose up --build -d` | ~10분 | 전체 재빌드 |

---

### 문제 5: 프론트엔드 변경사항이 반영되지 않음

#### 🔴 증상
React 코드를 수정했는데 브라우저에서 변경사항이 보이지 않음

#### 🔍 원인
- 브라우저 캐시
- Docker 빌드 캐시
- 컨테이너 재시작 안 함

#### ✅ 해결 방법

```bash
# 1. 프론트엔드 재빌드
docker-compose up --build -d frontend

# 2. 빌드 완료 대기 (약 30초)
sleep 30

# 3. 브라우저에서 강력 새로고침
# Chrome/Firefox: Ctrl + Shift + R
# Safari: Cmd + Shift + R
```

**빌드 캐시 문제 시**:
```bash
# 캐시 없이 재빌드
docker-compose build --no-cache frontend
docker-compose up -d frontend
```

---

### 문제 6: 데이터베이스 연결 실패

#### 🔴 증상
백엔드 로그에 `Connection refused` 에러

```bash
docker-compose logs backend --tail=50
# 출력: java.sql.SQLException: Connection refused
```

#### 🔍 원인
MariaDB 컨테이너가 완전히 시작되기 전에 백엔드가 접속 시도

#### ✅ 해결 방법

```bash
# 1. 컨테이너 순서 확인
docker-compose ps

# 2. MariaDB가 "Up"인지 확인
# mariadb    Up (healthy)

# 3. MariaDB 재시작
docker-compose restart mariadb

# 4. 백엔드 재시작
docker-compose restart backend

# 5. 연결 확인
docker exec mariadb mysql -u appuser -papppassword appdb -e "SELECT 1;"
```

**근본 해결** (docker-compose.yml):
```yaml
backend:
  depends_on:
    mariadb:
      condition: service_healthy
```

---

## 사용한 Bash 명령어

### Docker 조작

#### 1. 컨테이너 중지 및 삭제
```bash
# 모든 컨테이너 중지 및 삭제
docker-compose down

# 설명:
# - docker-compose.yml에 정의된 모든 컨테이너 중지
# - 컨테이너와 네트워크 삭제
# - 볼륨과 이미지는 유지됨
```

#### 2. 컨테이너 재빌드 및 시작
```bash
# 모든 컨테이너를 재빌드하여 시작 (백그라운드 모드)
docker-compose up --build -d

# 설명:
# --build: 이미지를 강제로 재빌드
# -d: 데타치 모드 (백그라운드 실행)
```

#### 3. 특정 컨테이너만 재빌드
```bash
# 백엔드만 재빌드
docker-compose up --build -d backend

# 프론트엔드만 재빌드
docker-compose up --build -d frontend

# 설명:
# 특정 서비스명을 지정하면 해당 컨테이너만 재빌드
```

#### 4. 컨테이너 재시작
```bash
# 백엔드 재시작 (재빌드 없음)
docker-compose restart backend

# 설명:
# 코드가 변경되지 않은 경우 빠른 재시작
# 이미지 재빌드는 하지 않음
```

#### 5. 컨테이너 상태 확인
```bash
# 실행 중인 컨테이너 표시
docker-compose ps

# 설명:
# NAME, IMAGE, STATUS, PORTS 등의 정보 표시
```

---

### 로그 확인

#### 6. 컨테이너 로그 표시
```bash
# 백엔드의 최근 50줄 로그 표시
docker-compose logs backend --tail=50

# 설명:
# --tail=N: 마지막 N줄만 표시
# -f 추가 시 실시간으로 추적
```

#### 7. 실시간 로그 모니터링
```bash
# 백엔드 로그를 실시간으로 표시
docker-compose logs -f backend

# 설명:
# -f: follow (새 로그가 추가되면 자동 표시)
# Ctrl+C로 종료
```

#### 8. 에러 로그 필터링
```bash
# 에러만 추출
docker-compose logs backend --tail=100 | grep -i "error\|exception\|failed"

# 설명:
# | grep: 파이프로 grep 명령어에 전달
# -i: 대소문자 구분 안 함
# \|: OR의 의미 (error 또는 exception 또는 failed)
```

---

### 데이터베이스 조작

#### 9. MariaDB 컨테이너 접속
```bash
# MariaDB 컨테이너에서 MySQL 클라이언트 실행
docker exec -it mariadb mysql -u appuser -papppassword appdb

# 설명:
# docker exec: 실행 중인 컨테이너에서 명령어 실행
# -it: 인터랙티브 모드 (대화형)
# mariadb: 컨테이너 이름
# mysql: MySQL 클라이언트 명령어
# -u appuser: 사용자명
# -papppassword: 비밀번호 (-p 바로 뒤, 공백 없음)
# appdb: 데이터베이스명
```

#### 10. SQL 쿼리 직접 실행
```bash
# 사용자 테이블 내용 표시
docker exec mariadb mysql -u appuser -papppassword appdb \
  -e "SELECT id, username, name, email FROM users LIMIT 5;"

# 설명:
# -e: execute (SQL문 직접 실행)
# \: 줄 연속 (긴 명령어를 여러 줄로 분할)
```

#### 11. 게시글 데이터 확인
```bash
# 게시글 테이블 내용 표시
docker exec mariadb mysql -u appuser -papppassword appdb \
  -e "SELECT id, title, user_id, board_id, created_at FROM posts LIMIT 5;"
```

---

### API 테스트

#### 12. API 엔드포인트 테스트
```bash
# GET 요청으로 데이터 가져오기
curl -s http://localhost:8080/api/posts/board/1

# 설명:
# curl: HTTP 요청 전송
# -s: 사일런트 모드 (진행 바 숨김)
```

#### 13. JSON 정렬 표시
```bash
# JSON을 정렬하여 표시
curl -s http://localhost:8080/api/posts/board/1 | python3 -m json.tool

# 설명:
# |: 파이프 (앞 명령어 출력을 다음 명령어에 전달)
# python3 -m json.tool: JSON 정렬하여 표시
```

#### 14. JSON 일부 추출
```bash
# 첫 번째 게시글만 표시
curl -s http://localhost:8080/api/posts/board/1 | \
  python3 -c "import sys, json; data=json.load(sys.stdin); \
  print(json.dumps(data[0], indent=2, ensure_ascii=False))"

# 설명:
# python3 -c: Python 코드 직접 실행
# json.load: JSON 파싱
# json.dumps: JSON 정렬 (indent=2로 2칸 들여쓰기)
```

---

### 파일 확인

#### 15. 컨테이너 내부 파일 목록
```bash
# Tomcat의 webapps 디렉토리 표시
docker exec spring-backend ls -la /usr/local/tomcat/webapps/

# 설명:
# ls: 파일 목록 표시
# -l: 상세 표시 (권한, 크기, 날짜 등)
# -a: 숨김 파일도 표시
```

#### 16. 특정 파일 검색
```bash
# WAR 파일 찾기
docker exec spring-backend ls -la /usr/local/tomcat/webapps/ | grep ej2

# 설명:
# grep: 지정한 문자열을 포함한 줄만 표시
```

---

### 기타 유용한 명령어

#### 17. 프로세스 대기
```bash
# 10초 대기 후 명령어 실행
sleep 10 && curl http://localhost:8080/api/posts/board/1

# 설명:
# sleep N: N초 대기
# &&: 앞 명령어가 성공하면 다음 실행
```

#### 18. 여러 명령어 연결
```bash
# 컨테이너 중지 → 재빌드 → 시작
docker-compose down && docker-compose up --build -d

# 설명:
# &&: 앞 명령어가 성공하면 다음 실행
# ;: 앞 명령어 결과와 관계없이 다음 실행
```

#### 19. 파일 내용 표시
```bash
# 파일의 처음 50줄 표시
head -50 /path/to/file

# 파일의 마지막 50줄 표시
tail -50 /path/to/file

# 실시간으로 파일 모니터링
tail -f /path/to/logfile
```

---

## 파일 변경 목록

### 백엔드 (Java/Spring)

| 파일 경로 | 변경 내용 | 타입 |
|----------|----------|------|
| `backend/src/main/java/com/ej2/dto/PostDTO.java` | 작성자 닉네임 포함 DTO | 신규 |
| `backend/src/main/java/com/ej2/service/PostService.java` | DTO 변환 로직 추가 | 수정 |
| `backend/src/main/java/com/ej2/controller/PostController.java` | DTO 반환으로 변경 | 수정 |
| `backend/src/main/java/com/ej2/config/WebConfig.java` | Jackson 날짜 설정 변경 | 수정 |

### 프론트엔드 (React)

| 파일 경로 | 변경 내용 | 타입 |
|----------|----------|------|
| `frontend/src/App.js` | 로고 링크화, 메뉴 삭제, 라우트 추가 | 수정 |
| `frontend/src/App.css` | 로고 스타일링 추가 | 수정 |
| `frontend/src/pages/Main/MainPage.js` | 닉네임 표시, 통계 형식 변경 | 수정 |
| `frontend/src/pages/Board/PostDetailPage.js` | 수정/삭제 버튼 추가 | 수정 |
| `frontend/src/pages/Board/PostDetailPage.css` | 버튼 스타일 추가 | 수정 |
| `frontend/src/pages/Board/PostListPage.js` | 닉네임 표시 | 수정 |
| `frontend/src/pages/Board/PostEditPage.js` | 게시글 수정 페이지 | 신규 |

---

## 초보자용 가이드

### 🔰 개발 환경 시작하기

```bash
# 1. 프로젝트 디렉토리로 이동
cd /Users/yunsu-in/Downloads/EJ2

# 2. 모든 컨테이너 시작
docker-compose up -d

# 3. 시작 확인
docker-compose ps

# 예상 출력:
# NAME             STATUS
# mariadb          Up
# react-frontend   Up
# spring-backend   Up
```

### 🔰 코드 변경 후 재시작 방법

#### 프론트엔드(React) 변경 시:
```bash
# 프론트엔드만 재빌드
docker-compose up --build -d frontend

# 약 30초~1분 대기
sleep 30

# 브라우저에서 새로고침: http://localhost:3000
```

#### 백엔드(Java) 변경 시:
```bash
# 백엔드만 재빌드
docker-compose up --build -d backend

# 약 1~2분 대기
sleep 60

# API 테스트
curl http://localhost:8080/api/posts/board/1
```

### 🔰 문제 발생 시 확인 순서

```bash
# 단계 1: 컨테이너 상태 확인
docker-compose ps
# → 모든 컨테이너가 "Up" 상태인지 확인

# 단계 2: 로그 확인
docker-compose logs backend --tail=50
# → 에러 메시지 찾기

# 단계 3: 데이터베이스 확인
docker exec mariadb mysql -u appuser -papppassword appdb \
  -e "SELECT COUNT(*) FROM posts;"
# → 데이터 존재 확인

# 단계 4: 완전 재시작
docker-compose down
docker-compose up --build -d
```

### 🔰 자주 발생하는 에러와 해결방법

#### 에러 1: "Cannot connect to Docker daemon"
```bash
# Docker가 실행되지 않음
# → Docker Desktop을 실행하세요
```

#### 에러 2: "Port 8080 is already in use"
```bash
# 포트가 이미 사용 중
# → 기존 컨테이너 중지
docker-compose down

# 여전히 문제가 있으면 다른 프로세스 확인
lsof -i :8080
# → 프로세스 ID 확인 후 종료
```

#### 에러 3: "Build failed"
```bash
# 빌드 캐시 삭제
docker-compose build --no-cache backend

# 여전히 실패하면 모두 삭제 후 재구축
docker-compose down -v
docker-compose up --build -d
```

---

## 요약

### 오늘 추가한 주요 기능:
1. ✅ 닉네임 표시 (사용자명 → 실명)
2. ✅ 날짜/시간 올바른 표시 (ISO 문자열 형식)
3. ✅ 인기글 통계 표시 개선
4. ✅ 로고 클릭 가능 및 스타일링
5. ✅ 게시글 삭제 기능 (자신의 게시글만)
6. ✅ 게시글 수정 기능 (자신의 게시글만)

### 사용한 주요 명령어:
- `docker-compose up --build -d`: 재빌드 및 시작
- `docker-compose logs backend`: 로그 확인
- `docker exec mariadb mysql`: DB 확인
- `curl`: API 테스트

### 접속 URL:
- 프론트엔드: http://localhost:3000
- 백엔드 API: http://localhost:8080/api
- 데이터베이스: localhost:3306

---

**작성일시**: 2026-01-23
**작성자**: Claude (AI 어시스턴트)
**대상**: 초보자~중급자 개발자
